---
id: llm-flashattention-v1
name: FlashAttention-V1
year: 2022
tags: Attention, IO-aware, CUDA, Transformer
---

### 核心问题

#### Attention 符号速览
Transformer 里的 self-attention 可以先按下面三步理解：

$$
S = QK^T,
\qquad
P = \operatorname{softmax}(S),
\qquad
O = P V
$$

- `Q`：Query，表示当前 token 想要查询什么信息。
- `K`：Key，表示每个 token 可以被匹配的特征。
- `V`：Value，表示每个 token 真正要贡献出去的内容向量。
- `S = QK^T`：计算 token 之间的匹配分数。
- `P = softmax(S)`：把匹配分数变成注意力权重。
- `P @ V` 或 `P V`：矩阵乘法，用注意力权重 `P` 对 `V` 做加权求和，得到输出 `O`。

通俗地说，`P` 决定“该看谁、看多少”，`V` 是“被看的内容”，`P @ V` 就是“按权重把内容混合起来”。FlashAttention 优化的正是 `S -> P -> O` 这条链路里大量中间矩阵的读写。

#### 从普通 Attention 到 FlashAttention
- Attention 优化大致经历了几类思路：标准 Attention、Memory-efficient Attention，以及 FlashAttention 这类 IO-aware exact attention。
- 标准 Attention 好理解，但会把 `QK^T` 和 softmax 后的概率矩阵都写到显存里。
- Memory-efficient Attention 已经开始减少反向传播中的显存保存，但 FlashAttention 进一步把前向和反向都围绕“少访问 HBM”来重排。
- 通俗地说，FlashAttention 不是“少算一点 attention”，而是“同样的 attention，别把中间草稿纸反复搬进搬出显存”。

#### 标准 Attention 的瓶颈
- 背景：Transformer 里的自注意力通常写成 `S = QK^T / sqrt(d)`、`P = softmax(S)`、`O = PV`。
- 问题：`S` 和 `P` 都是序列长度平方级别的矩阵，长序列下会占用大量 HBM 显存读写带宽。
- 关键观察：很多 Attention kernel 的瓶颈不是算力，而是把中间矩阵反复写入和读出 HBM。
- FlashAttention-V1 的目标：在保持 exact attention 结果的前提下，减少 HBM 访问，不近似、不稀疏化、不改变模型数学定义。

标准实现为了方便框架调度，会把 `QK^T`、softmax 结果和 dropout/mask 之后的概率矩阵显式物化出来。这样代码清楚，但会产生大量中间读写。FlashAttention-V1 的核心思路是把这些中间状态留在 SRAM/shared memory 和寄存器里，以 block 为单位边算边归约。

在 MHA profiling 里，scaled dot-product attention 经常很耗时。原因不是 `QK^T` 这次矩阵乘本身特别难，而是 `QK^T` 的形状是 `[batch_size, num_heads, seqlen, seqlen]`。`seqlen` 一变长，这个中间矩阵就按平方级膨胀；前向要写，softmax 要读写，反向还可能为了算梯度再读一次。这就是“显存流量”成为瓶颈的根源。

### IO-aware 思路

#### 从伪融合到真正融合
- 早期很多 fused attention 更像是“把 matmul、scale、mask、softmax、matmul 的调用封装到一个接口里”。
- 这种封装能减少框架层的调度开销，但如果中间的 `S` 和 `P` 仍然落到 HBM，就没有真正解决 memory traffic。
- FlashAttention-V1 的融合更彻底：它把 score、softmax 和 `P @ V` 放进同一个分块流程里，尽量让中间结果只在片上存储中短暂停留。
- 通俗地说，伪融合像是把多个窗口排到一个柜台后面，流程还是一站一站搬材料；真正融合是把材料放在同一张工作台上连续加工。

[full-image: flashattn_banner.jpg]
[caption: FlashAttention 将 Attention 计算按 block 重排，让 score、softmax 和 V 加权累加尽量在片上完成，避免完整中间矩阵反复进出 HBM。]

#### HBM 与 SRAM 的角色
- HBM：容量大但访问慢，适合存放完整的 `Q`、`K`、`V` 和最终输出 `O`。
- SRAM/shared memory：容量小但访问快，适合缓存当前 block 的 `Q`、`K`、`V` tile。
- 寄存器：保存每一行 softmax 的运行最大值、归一化分母和局部输出累加。
- 设计重点：让每个 `K/V` tile 被加载后尽可能多地复用，避免完整 attention score 矩阵落回 HBM。

FlashAttention-V1 不是减少乘加次数，而是减少 IO。它仍然计算完整的 attention，只是把计算顺序改成分块扫描：每次取一块 `Q` 和一块 `K/V`，完成局部 score、局部 softmax 统计和输出累加，然后继续处理下一块。

#### 为什么能省显存
- 标准 Attention 会显式保存 `N x N` 的 score/probability 矩阵。
- FlashAttention-V1 不保存完整 `N x N` 中间矩阵，只保存每行的少量统计量。
- 前向阶段通常只需要保存输出、softmax 的归一化统计，以及反向需要重算的必要信息。
- 显存节省来自“少存中间矩阵”，速度提升来自“少访问慢速 HBM”。

这里最大的难点是 softmax。softmax 是 row-wise 操作：一行里每个元素的结果，都依赖这一整行的最大值和分母。如果按普通写法，必须先把一整行 score 算完并保存下来，后面的 `P @ V` 才能继续。FlashAttention-V1 的做法是维护每行的在线统计量，让“一整行”可以被拆成多个小块陆续处理。

### 分块算法

把 Attention 改成分块计算后，真正麻烦的地方不是 `QK^T` 的小块矩阵乘。矩阵乘本来就可以按 tile 做，难点在 softmax：每一行的 softmax 分母依赖这一整行所有 score。如果只是把每个小块各自 softmax，再把结果拼起来，数学结果会变掉。所以下面的流程先解决 softmax 的分块等价性，再看它如何嵌入 FlashAttention 的前向扫描。

#### Online Softmax
普通 softmax 通常先拿到一整行 score，再减最大值、指数化、求和。Online Softmax 做的是同一件事，只是把“一次性拿完整行”改成“按 block 逐段看到这一行”。每看到一个新 block，它都会更新这一行的最大值和归一化分母，并把旧的输出累加按比例缩放到新的归一化尺度。

先用一个只有 4 个 score 的例子看直觉。假设某一行 `QK^T` 得到：

$$
s = [0.1,\ 0.2,\ 0.3,\ 0.4],
\qquad
v = [v_0,\ v_1,\ v_2,\ v_3]
$$

完整 softmax 之后再乘 `V`，等价于下面这个加权平均：

$$
o =
\frac{\sum_{i=0}^{3} e^{s_i} v_i}{\sum_{i=0}^{3} e^{s_i}}
=
\frac{e^{0.1}v_0 + e^{0.2}v_1 + e^{0.3}v_2 + e^{0.4}v_3}
{e^{0.1} + e^{0.2} + e^{0.3} + e^{0.4}}
$$

如果一次只能分块看到两个 score，第一轮只能得到前两个元素的局部归一化结果：

$$
Z_1 = e^{0.1} + e^{0.2},
\qquad
o_1 = \frac{e^{0.1}v_0 + e^{0.2}v_1}{Z_1}
$$

第二轮看到 `[0.3, 0.4]` 后，总分母变成：

$$
Z_2 = Z_1 + e^{0.3} + e^{0.4}
$$

这时不能直接把第二块的结果拼上去，而是要把旧输出 `o_1` 从旧分母 `Z_1` 缩放到新分母 `Z_2`，再加上新块贡献：

$$
o_2 =
\frac{Z_1}{Z_2} o_1
+
\frac{e^{0.3}v_2 + e^{0.4}v_3}{Z_2}
$$

在代码里，`pre_sum` 对应旧分母 `Z_1`，`cur_sum` 对应新分母 `Z_2`。这个关系说明：旧结果不是丢掉，而是换一个新的归一化尺度继续累计。

```python
# 单行 online softmax 的概念示意。
# x_block 是当前看到的一段 score，v_block 是对应的 V block。

def online_softmax_update_row(x_block, v_block, old_m, old_l, old_o):
    # 当前 block 内最大值。
    block_m = max(x_block)

    # 新最大值是旧最大值和当前 block 最大值的较大者。
    new_m = max(old_m, block_m)

    # 旧分母要从 old_m 的尺度缩放到 new_m 的尺度。
    old_l_rescaled = old_l * exp(old_m - new_m)

    # 当前 block 的指数也按 new_m 的尺度计算，避免溢出。
    p = exp(x_block - new_m)

    # 新分母等于缩放后的旧分母加当前 block 的指数和。
    new_l = old_l_rescaled + sum(p)

    # 旧输出累加同样需要缩放，然后加上当前 block 对 V 的贡献。
    new_o = old_o * (old_l_rescaled / new_l) + (p @ v_block) / new_l

    return new_m, new_l, new_o
```

下面是不考虑数值稳定 `max` 缩放时的更直观版本，用一个最小例子展示分块累计。真实实现仍然要维护 `m`，避免 `exp(score)` 溢出。

```python
# 为了说明分块累计，先忽略 max trick。
# scores = [0.1, 0.2, 0.3, 0.4]，每次只处理两个元素。

pre_sum = 0.0
result = 0.0

# 第一块：[0.1, 0.2]
cur_sum = exp(0.1) + exp(0.2)
result = (exp(0.1) * v0 + exp(0.2) * v1) / cur_sum
pre_sum = cur_sum

# 第二块：[0.3, 0.4]
cur_sum = pre_sum + exp(0.3) + exp(0.4)

# 旧 result 原来除以 pre_sum；现在总分母变成 cur_sum，所以旧结果要缩放。
result = result * (pre_sum / cur_sum)

# 再加上当前块对输出的贡献。
result += (exp(0.3) * v2 + exp(0.4) * v3) / cur_sum
pre_sum = cur_sum
```

有了这个状态更新规则，FlashAttention 的前向扫描就比较自然了：每个 score block 算出来后，不把它写回 HBM，也不单独产出一块概率矩阵，而是立刻更新当前行的 `m/l/O` 状态。

#### 前向扫描
FlashAttention-V1 前向可以理解成两层 block 循环：外层遍历 `K/V` block，内层遍历 `Q` block。每个 `Q` block 会与当前 `K` block 做矩阵乘，得到一小块 score，然后立刻参与 Online Softmax 和输出累加。

```python
# FlashAttention-V1 前向流程的概念伪代码。
# 真实 CUDA kernel 会把 Q/K/V tile 放到 shared memory，并用线程块并行处理。

# O 保存最终 attention 输出。
O = zeros_like(Q)

# m 保存每一行目前见过的最大 score，用于稳定 softmax。
m = full((seq_len,), -inf)

# l 保存每一行 softmax 分母的累计值。
l = zeros((seq_len,))

for kv_start in range(0, seq_len, block_k):
    # 取当前 K/V tile。这个 tile 会被多个 Q block 复用。
    K_tile = K[kv_start : kv_start + block_k]
    V_tile = V[kv_start : kv_start + block_k]

    for q_start in range(0, seq_len, block_q):
        # 取当前 Q tile。
        Q_tile = Q[q_start : q_start + block_q]

        # 只计算一个小块 score，不把完整 N x N score 矩阵写回 HBM。
        scores = Q_tile @ K_tile.T * scale

        # 如果是 causal attention，要在这里对未来 token 做 mask。
        scores = apply_mask_if_needed(scores, q_start, kv_start)

        # 用 Online Softmax 更新当前 Q 行的最大值、分母和输出累加。
        m, l, O = online_softmax_update(scores, V_tile, m, l, O, q_start)
```

从直觉上看，`Q/K/V/O` 都被切成小块。`K/V` 的块在外层循环中被拿进来，`Q/O` 的块在内层循环中更新。每处理一个 `K/V` 小块，就得到当前 score 行的一段贡献，然后立即通过 Online Softmax 把它合并到输出 `O`。这样既不需要保存完整 `S`，也不需要等完整 `P` 矩阵生成后再做 `P @ V`。

### 反向传播

#### 重算而不是保存巨大中间矩阵
- 标准做法倾向保存 softmax 概率矩阵，反向时直接读取。
- FlashAttention-V1 选择保存更小的统计量，在反向中按 block 重算 score 和 softmax。
- 反向重算增加了一些算力开销，但避免了巨大的 HBM 读写。
- 在 GPU 上，额外计算通常比额外 HBM IO 更便宜，因此整体仍然更快。

这个取舍是 FlashAttention-V1 的核心工程判断：用可控的重算换掉昂贵的中间矩阵存储。对于长序列，`N x N` 中间矩阵非常大，减少它的读写比少做一点重算更重要。

### 适用场景

#### 什么时候收益明显
- 长序列训练和推理：序列越长，`N x N` 中间矩阵越大，IO 优化越明显。
- head dimension 适配 kernel 的场景：常见的 64、128 等维度更容易跑到优化路径。
- A100/H100 等高带宽 GPU：FlashAttention 能更好地把 SRAM/shared memory 和 Tensor Core 协同起来。
- 需要 exact attention 的模型：相比近似 attention，它不改变 attention 数学结果，更容易替换标准实现。

#### 需要注意的边界
- 短序列或小 batch：kernel launch、调度和其他算子可能占主要时间，收益不一定明显。
- 非典型 mask 或 shape：如果 kernel 没有覆盖对应模式，可能回退到普通 attention。
- 端到端速度：Attention 变快不代表整个模型同比例变快，layernorm、MLP、通信和 KV cache 管理也会影响总耗时。
- 数值细节：虽然是 exact attention，混合精度、mask、dropout 和累加路径仍然需要和框架实现对齐。

### 工程用法

#### 融合前后对比
融合前，scaled dot-product attention 可以拆成 `matmul -> scale -> mask -> softmax -> matmul`。这些算子各自执行时，中间矩阵很容易写回 HBM。融合后，FlashAttention-V1 把它们组织成一个 IO-aware kernel：score 小块刚算出来，就立刻做 mask、online softmax 和 `V` 的加权累积。

这可以理解为“流水线换位置”：不是先把所有半成品堆到仓库，再让下一个工序取走；而是每个小块在同一个工位上尽量加工完，只把必要的最终结果和少量统计量留下。

#### PyTorch 调用形态
现代框架通常会在 `scaled_dot_product_attention` 或上层库中自动选择 FlashAttention 类 kernel。工程上更常见的做法不是手写 CUDA，而是保证 dtype、mask、head dimension 和设备满足 kernel 条件。

```python
# PyTorch 侧的典型调用形态。
# 在支持的 GPU、dtype 和 shape 下，后端可能选择 FlashAttention 类 kernel。

import torch
import torch.nn.functional as F

# q/k/v 常见形状为 [batch, heads, seq_len, head_dim]。
q = torch.randn(batch, heads, seq_len, head_dim, device="cuda", dtype=torch.float16)
k = torch.randn(batch, heads, seq_len, head_dim, device="cuda", dtype=torch.float16)
v = torch.randn(batch, heads, seq_len, head_dim, device="cuda", dtype=torch.float16)

# is_causal=True 表示自回归模型常见的 causal mask。
out = F.scaled_dot_product_attention(q, k, v, is_causal=True)
```

#### 与普通 Attention 的差异
```python
# 普通写法会显式产生 scores 和 probs 这两个 N x N 中间矩阵。
scores = q @ k.transpose(-2, -1) * scale
probs = torch.softmax(scores, dim=-1)
out = probs @ v

# FlashAttention 的 kernel 会把上面三步融合到分块扫描里。
# Python 层看起来仍然是 attention，但底层避免了完整 scores/probs 落到 HBM。
out = flash_attention(q, k, v, causal=True)
```

### References
- [FlashAttention-V1 微信文章](https://mp.weixin.qq.com/s/jPJuF2BCymNJqdzsQbPy1w)
- [FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness](https://arxiv.org/abs/2205.14135)
- [OpenReview: FlashAttention](https://openreview.net/forum?id=H4DqfPSibmx)
- [Dao-AILab flash-attention GitHub](https://github.com/Dao-AILab/flash-attention)
